import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
    box: {
        display: 'flex',
        justifyContent: 'center',
        paddingLeft: 200,
        paddingRight: 200,
        paddingTop: 100
    }
}));
